# Tasks: fix-contact-form-ux

- [x] **1. Agrupar elementos y añadir espaciado en encabezados colapsables:** En `src/pages/contacto.astro`, modificar el HTML interno de los `<h2>` de las secciones "Calificación de Requerimientos" y "Detalles de Contacto". Envolver el texto principal y la etiqueta "Opcional" en un `div` con flexbox (`flex items-center gap-2`), y asegurar que el botón contenedor principal tenga `gap-4` para un distanciamiento de al menos `1rem`.
- [x] **2. Corregir funcionalidad de botones (Evitar submit prematuro) y UX de cursor:** En `src/pages/contacto.astro`, localizar los botones de acordeón (`<button id="toggle-requerimientos">` y `<button id="toggle-detalles">`). Añadir el atributo estricto `type="button"` (para que no actúen como *submit*) y agregar la clase `cursor-pointer` a su lista de clases.
- [x] **3. Validar envío del formulario:** Realizar una prueba manual para verificar que hacer clic en los botones de acordeón no dispara las validaciones HTML5 requeridas ni envía la petición POST, confirmando que la lógica solo corre cuando se presiona explícitamente "Enviar Solicitud".
